class AppDurConst {
  AppDurConst._();
  static const Duration transissionDuration = Duration(milliseconds: 100);
  static const Duration halfSecDuration = Duration(milliseconds: 500);
  static const Duration fiftymlSecDuration = Duration(milliseconds: 50);
  static const Duration refreshDuration = Duration(seconds: 3);
}
